(ns tilemap-editor.assets
  (:require pixils))

(defbundle editor-assets
  {:images {:spritesheet {:file-name "assets/simples_pimples.png"
                          :transparency-color "#5a5268"}}})

(defbundle-dynamic project-assets)
