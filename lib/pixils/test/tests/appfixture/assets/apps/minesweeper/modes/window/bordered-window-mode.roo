(ns pixils.test.app.minesweeper.core
  (:require pixils
            [pixils.color :as c]))

(defmode window-mode
  {:focusable true
   :style {:border {:thickness 1
                    :color c/BLACK}}
   :children [{:mode 'menu-bar-mode}
              {:mode 'game-mode}]})
