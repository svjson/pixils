(ns boulder-rush.core
  (:require pixils
            boulder-rush.init-mode
            boulder-rush.sounds
            [pixils.color :as c]))

(defprogram boulder-rush
  {:display {:resolution {:w 320 :h 200}
             :scaling :scaling/fit
             :align :align/center
             :background c/BLACK}
   :target-frame-rate 40
   :initial-mode 'init-mode})
