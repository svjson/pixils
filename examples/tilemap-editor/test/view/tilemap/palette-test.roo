(ns tilemap-editor.view.tilemap.palette-test
  (:require proof.core
            [tilemap-editor.view.tilemap.palette :as palette]))

(deftest tile-palette-is-disabled-when-empty
  (is (= true
         (:disabled? (palette/tile-select-grid-state
                      {:tilesets []
                       :layers [{:id :ground
                                 :data-kind :tile-ref
                                 :tileset :colors}]
                       :selected-layer-index 0})))))

(deftest tile-palette-is-disabled_without_layers
  (let [tilesets [{:id :colors
                   :label "Colors"
                   :tiles [{:id :grass
                            :name "Grass"}]}]]
    (is (= true
           (:disabled? (palette/tile-select-grid-state
                        {:tilesets tilesets
                         :layers []
                         :selected-layer-index nil}))))
    (is (not= true
              (:disabled? (palette/tile-select-grid-state
                           {:tilesets tilesets
                            :layers [{:id :ground
                                      :data-kind :tile-ref
                                      :tileset :colors}]
                            :selected-layer-index 0}))))))

(deftest tile-palette-state-ignores_stale_parent_disabled_flag_when_options_exist
  (let [tilesets [{:id :colors
                   :label "Colors"
                   :tiles [{:id :grass
                            :name "Grass"}]}]]
    (is (not= true
              (:disabled? (palette/tile-select-grid-state
                           {:tilesets tilesets
                            :layers [{:id :ground
                                      :data-kind :tile-ref
                                      :tileset :colors}]
                            :selected-layer-index 0
                            :disabled? true}))))))

(deftest terrain-palette-grid-lists_terrain_values
  (let [grid-state (palette/tile-select-grid-state
                    {:terrain-sets [{:id :overworld
                                     :terrains [{:id :grass
                                                 :label "Grass"}]}]
                     :layers [{:id :scene/terrain
                               :data-kind :terrain
                               :terrain-set :overworld}]
                     :selected-layer-index 0
                     :selected-tile :grass})]
    (is (not= true (:disabled? grid-state)))
    (is (= [{:value :grass
             :label "Grass"}]
           (:options grid-state)))
    (is (= [0]
           (palette/selected-tile-indices
            {:terrain-sets [{:id :overworld
                             :terrains [{:id :grass
                                         :label "Grass"}]}]
             :layers [{:id :scene/terrain
                       :data-kind :terrain
                       :terrain-set :overworld}]
             :selected-layer-index 0
             :selected-tile :grass})))))

(deftest tile-palette-content-height-grows_by_grid_rows
  (is (= 0 (palette/tile-select-grid-content-height 0 4)))
  (is (= 16 (palette/tile-select-grid-content-height 4 4)))
  (is (= 32 (palette/tile-select-grid-content-height 5 4))))

(deftest tile-palette-list-wraps_grid_in_scroll-pane
  (let [child (palette/tile-palette-list-child)]
    (is (= 'tile-select-grid-scroll-pane (:mode child)))
    (is (not (nil? (-> child :state :selected-tile))))))

(deftest tile-palette-scroll-content-style-offsets_content
  (let [style (palette/tile-select-grid-scroll-content-style
               {:offset 32
                :content-height 96})]
    (is (= :absolute (:position style)))
    (is (= -32 (:top style)))
    (is (= 96 (:height style)))))

(deftest brush-palette-is-disabled-when-empty
  (is (= true
         (:disabled? (:state (palette/brush-palette-list-box-child
                              []
                              false
                              nil))))))

(deftest brush-palette-selects_first_available_brush
  (let [brushes [{:id :stamp
                  :label "Stamp"
                  :kind :pattern}]]
    (is (= [0]
           (:selected-indices
            (:state (palette/brush-palette-list-box-child brushes
                                                          false
                                                          nil)))))))
