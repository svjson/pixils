(ns tilemap-editor.view.masks.forms)

(def mask-set-size-text
  "Field labels for the mask-set dimension form."
  {:width "Width"
   :height "Height"})

(def mask-set-size-layout
  "Layout for the mask-set dimension form."
  [:width :height])
